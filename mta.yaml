_schema-version: "3.2"
ID: tutorial
description: Tutorial Router
version: 0.0.1
modules:
- name: tutorial-router
  type: approuter.nodejs
  path: router
  requires:
  - name: tutorial-html5-repo-runtime
  - name: tutorial-uaa
  - name: tutorial-destination-service
    group: destinations
    properties:
      forwardAuthToken: false
      name: ui5
      url: https://ui5.sap.com
  parameters:
    disk-quota: 256M
    memory: 256M
- name: tutorial-app-content
  type: com.sap.application.content
  path: .
  requires:
  - name: tutorial-repo-host
    parameters:
      content-target: true
  build-parameters:
    build-result: resources
    requires:
    - artifacts:
      - tutorialfiori.zip
      name: tutorialfiori
      target-path: resources/
- name: tutorialfiori
  type: html5
  path: tutorial-fiori
  build-parameters:
    build-result: dist
    builder: custom
    commands:
    - npm install
    - npm run build:cf
    supported-platforms: []
resources:
- name: tutorial-uaa
  type: org.cloudfoundry.managed-service
  parameters:
    config:
      tenant-mode: dedicated
      xsappname: tutorial-${space-guid}
    service: xsuaa
    service-plan: application
- name: tutorial-html5-repo-runtime
  type: org.cloudfoundry.managed-service
  parameters:
    service: html5-apps-repo
    service-plan: app-runtime
- name: tutorial-destination-service
  type: org.cloudfoundry.managed-service
  parameters:
    config:
      HTML5Runtime_enabled: false
      init_data:
        instance:
          destinations:
          - Authentication: NoAuthentication
            Name: ui5
            ProxyType: Internet
            Type: HTTP
            URL: https://ui5.sap.com
          existing_destinations_policy: update
      version: 1.0.0
    service: destination
    service-name: tutorial-destination-service
    service-plan: lite
- name: tutorial-repo-host
  type: org.cloudfoundry.managed-service
  parameters:
    service: html5-apps-repo
    service-name: tutorial-html5-service
    service-plan: app-host
parameters:
  deploy_mode: html5-repo
  enable-parallel-deployments: true
